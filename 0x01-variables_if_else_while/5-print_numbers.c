#include <stdio.h>

/**
 * main - Entry point
 * Desc: Entry
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ls;

	for (ls = 48 ; ls <= 57 ; ls++)

		putchar(ls);


	putchar('\n');

	return (0);
}
