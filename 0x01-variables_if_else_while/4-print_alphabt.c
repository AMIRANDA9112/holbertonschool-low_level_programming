#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - start of the program
 *
 * Return: 0
 */
int main(void)
{
	char ls;

	for (ls = 'a' ; ls <= 'z' ; ch++)
{
	if (ls != 'e' ; ls != 'q')
	putchar (ls);
}
	putchar('\n');
	return (0);
}
