#include <time.h>
#include <stdio.h>
#include <stdio.h>

/**
 * main - entry point
 * Return: always 0 (succes)
 */

	int main(void)

{
	int xs;

	for (xs = 0; l < 10; xs++)
	putchar(48 + xs);
	putchar(10);
	return(0);
}
