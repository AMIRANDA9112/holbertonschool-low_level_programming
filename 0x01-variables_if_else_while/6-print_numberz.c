#include <time.h>
#include <stdio.h>
#include <stdio.h>

/**
 * main - entry point
 * Return: always 0 (succes)
 */

	int main(void)

{
	int ls;

	for (ls = 0; ls < 10; ls++)
	putchar(48 + ls);
	putchar(10);
	return(0);
}
